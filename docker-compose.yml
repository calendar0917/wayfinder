services:
  homepage:
    build: .
    container_name: homepage
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    env_file: .env.local
    pid: host
    restart: unless-stopped
