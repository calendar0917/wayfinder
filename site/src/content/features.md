---
title: 功能特性
---

## 功能特性

### AI 助手

用对话操控仪表盘——添加书签、调整布局、更换主题、配置集成，全部通过自然语言完成

### 书签管理

拖拽排序、嵌套子分组、标签过滤、自动获取图标、HTTP 状态检测

### 实时数据集成

轮询任意 JSON API，以类型化格式展示（字节、百分比、温度等），内置常用服务模板

### Docker 集成

直接在仪表盘上监控容器运行状态

### 多页面

将分组组织到不同标签页中——工作、个人、家庭服务器

### 主题系统

明暗自动切换，CSS 变量系统，支持自定义 CSS 注入
